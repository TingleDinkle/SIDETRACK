node_modules/
*.log
# Local headless smoke-test scaffolding (not part of the game)
__*.html
# Scratch test files (e.g. left by agents) — never part of the repo
/test_*.mjs
/test-*.mjs
/test_*.js
/test-*.js
/test_*.json
/test-*.json
# Compiled JS belongs in dist/, never in src/
src/*.js
src/*.js.map
# Source 3D art pack (18MB, not used at runtime — bake to PNG sprites instead).
src/Models/
# Source UI kit (atlases/fonts/PDF) — runtime slices live in assets/ui + assets/fonts.
src/2D Game UI Kit/
